/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/lsb-alex-prins-real-estate-agent-compass-dallas',
  images: { unoptimized: true },
};
module.exports = nextConfig;
